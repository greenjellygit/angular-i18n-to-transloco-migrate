import { CamelCaseDirective } from './camel-case.directive';

describe('CamelCaseDirective', () => {
  it('should create an instance', () => {
    const directive = new CamelCaseDirective();
    expect(directive).toBeTruthy();
  });
});
